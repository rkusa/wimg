[package]
name = "wimg"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

# optimize for small code size
[profile.release]
opt-level = "s"
lto = true

[dependencies]
mozjpeg-sys = "1.0"
resize = "0.7"
rgb = "0.8"

[build-dependencies]
cbindgen = "0.20"
